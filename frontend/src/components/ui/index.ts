/**
 * Unified UI Component Library
 *
 * Import these components to ensure visual consistency
 * across all pages.
 *
 * Usage:
 * import { PageContainer, StatusCard, StatCard, Icon } from '@/components/ui';
 */

export { PageContainer } from './PageContainer';
export { StatusCard } from './StatusCard';
export { StatCard } from './StatCard';
export { EmptyState } from './EmptyState';
export { Icon } from './Icon';

// Re-export design system for convenience
export * from '../../theme/design-system';
